import base from './base';

const createCharacter = () => {
  // some call to base goes here...
  // in the future, it can be replaced with a call to a server API
  // and this can be tucked away there.
};

modules.exports = {
  createCharacter,
};
